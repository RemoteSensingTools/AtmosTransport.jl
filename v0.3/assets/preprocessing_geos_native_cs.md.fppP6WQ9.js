import{_ as s,o as i,c as t,an as a}from"./chunks/framework.BR9x3YLT.js";const u=JSON.parse('{"title":"GEOS native cubed-sphere","description":"","frontmatter":{},"headers":[],"relativePath":"preprocessing/geos_native_cs.md","filePath":"preprocessing/geos_native_cs.md","lastUpdated":null}'),n={name:"preprocessing/geos_native_cs.md"};function o(r,e,d,l,c,p){return i(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="GEOS-native-cubed-sphere" tabindex="-1">GEOS native cubed-sphere <a class="header-anchor" href="#GEOS-native-cubed-sphere" aria-label="Permalink to &quot;GEOS native cubed-sphere {#GEOS-native-cubed-sphere}&quot;">​</a></h1><p>The GEOS path takes <strong>GEOS-IT C180</strong> native NetCDF — the FV3 dynamical core&#39;s own cubed-sphere output — and writes a transport binary on the same cubed-sphere grid (no horizontal regrid). GEOS-FP native C720 hourly CTM files use the same source contract, with one file per UTC hour. It preserves native FV3 horizontal mass fluxes, balances them against raw next-hour dry-air endpoints, and diagnoses the vertical mass flux required by that discrete continuity target.</p><p>GEOS-FP C720 support covers the native hourly CTM layout (<code>GEOS.fp.asm.tavg_1hr_ctm_c0720_v72.YYYYMMDD_HH30.V01.nc4</code>; test fixtures may also use <code>HH00</code>). Surface/convection physics from the 0.25° GEOS-FP products can be attached by setting <code>[source] physics_dir</code>, <code>include_surface = true</code>, and <code>include_convection = true</code>; the day handle validates and embeds those payloads into the same transport binary.</p><h2 id="Why-column-balance-instead-of-Poisson-balance?" tabindex="-1">Why column balance instead of Poisson balance? <a class="header-anchor" href="#Why-column-balance-instead-of-Poisson-balance?" aria-label="Permalink to &quot;Why column balance instead of Poisson balance? {#Why-column-balance-instead-of-Poisson-balance?}&quot;">​</a></h2><p>A spectral preprocessor needs a Poisson balance step because winds synthesized from VO + D do <strong>not</strong> satisfy the continuity equation exactly on the discrete grid; balance closes the residual.</p><p>GEOS native MFXC / MFYC are <strong>already discrete-conservative</strong> — the FV3 dynamical core integrated them under its own discrete continuity. Running a Poisson balance on top would add a small spurious correction with no physical justification. The GEOS path therefore <strong>skips Poisson balance</strong> and instead runs a one-pass <strong>column balance</strong> (<code>balance_cs_column_mass_fluxes!</code>) to close the horizontal fluxes against the raw next-hour dry-air-mass endpoint, then diagnoses the vertical mass flux <code>cm</code> from those balanced fluxes via <code>diagnose_cs_cm!</code> (the same diagnostic used by the LL and RG spectral paths after their respective Poisson balances).</p><p>The endpoint convention is the <strong>raw dry endpoint</strong> rather than the endpoint implied by an FV3-style pressure fixer. The pressure-fixer endpoint can go slightly negative in thin upper layers; the raw endpoint is robust and the header records <code>&quot;geos_mass_endpoint&quot; =&gt; &quot;raw_dry_endpoint&quot;</code> for traceability.</p><h2 id="Required-input-per-day" tabindex="-1">Required input per day <a class="header-anchor" href="#Required-input-per-day" aria-label="Permalink to &quot;Required input per day {#Required-input-per-day}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Collection</th><th style="text-align:right;">Cadence</th><th style="text-align:right;">Variables</th></tr></thead><tbody><tr><td style="text-align:right;"><code>CTM_A1</code></td><td style="text-align:right;">hourly (window-averaged)</td><td style="text-align:right;"><code>MFXC</code>, <code>MFYC</code>, <code>DELP</code></td></tr><tr><td style="text-align:right;"><code>CTM_I1</code></td><td style="text-align:right;">hourly (instantaneous)</td><td style="text-align:right;"><code>PS</code>, <code>QV</code></td></tr><tr><td style="text-align:right;"><code>A1</code></td><td style="text-align:right;">hourly</td><td style="text-align:right;"><code>PBLH</code>, <code>USTAR</code>, <code>HFLUX</code>, <code>T2M</code> <em>(GEOS-IT native or GEOS-FP physics fallback, only if surface is enabled)</em></td></tr><tr><td style="text-align:right;"><code>A3mstE</code></td><td style="text-align:right;">3-hourly (window-averaged)</td><td style="text-align:right;"><code>CMFMC</code> <em>(only if convection is enabled)</em></td></tr><tr><td style="text-align:right;"><code>A3dyn</code></td><td style="text-align:right;">3-hourly (window-averaged)</td><td style="text-align:right;"><code>DTRAIN</code> <em>(only if convection is enabled)</em></td></tr></tbody></table><p>GEOS-IT file-naming convention: <code>GEOSIT.YYYYMMDD.&lt;COLLECTION&gt;.C180.nc</code>. GEOS-FP native CTM file-naming convention: <code>GEOS.fp.asm.tavg_1hr_ctm_c0720_v72.YYYYMMDD_HH30.V01.nc4</code>. The preprocessor needs the <strong>next day&#39;s hour-0</strong> for the last window&#39;s forward-flux endpoint, mirroring the spectral path; the trailing peek at <code>&lt;end+1&gt;</code> is unavoidable.</p><h2 id="GEOS-preprocessing-TOML" tabindex="-1">GEOS preprocessing TOML <a class="header-anchor" href="#GEOS-preprocessing-TOML" aria-label="Permalink to &quot;GEOS preprocessing TOML {#GEOS-preprocessing-TOML}&quot;">​</a></h2><p>A working GEOS-IT C180 → CS C180 preprocessing config has two TOML files: a small <strong>preprocessing TOML</strong> (per-run knobs) and a separate <strong>source descriptor TOML</strong> (shared with the runtime, declares the source&#39;s invariants):</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># config/preprocessing/geosit_c180_to_cs180.toml</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">source</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">toml               = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;config/met_sources/geosit.toml&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # source descriptor (below)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">root_dir           = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;~/data/AtmosTransport/met/geosit_c180/raw_catrine&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">include_surface    = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # PBL surface payload (pblh, ustar, hflux, t2m)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">include_convection = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # CMFMC + DTRAIN payload</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">include_vdiff_fields = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # GCHP VDIFF payload (u, v, T, qv at substep cadence)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">output</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">directory  = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;~/data/AtmosTransport/met/geosit/C180/preprocessed/merge025hpa_adaptive_f32&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mass_basis = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;dry&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                             # binary header</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">grid</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type                = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;cubed_sphere&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Nc                  = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">180</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">panel_convention    = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;geos_native&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">regridder_cache_dir = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;~/.cache/AtmosTransport/cr_regridding&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vertical</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">transform    = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;merge_above_pressure&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">threshold_pa = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">25.0</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                              # merge above 0.25 hPa → 64-level product</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">coefficients = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;config/geos_L72_coefficients.toml&quot;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># The default &quot;identity&quot; transform keeps all 72 levels; production</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># C180 uses &quot;merge_above_pressure&quot; with threshold_pa = 25.0 (0.25 hPa)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># to fold the very thin upper-mesosphere layers into one, which makes</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># the palindrome positivity budget feasible.</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">numerics</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">float_type            = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Float32&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dt_met_seconds        = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3600.0</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           # CTM cadence (hourly for GEOS-IT)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">substep_schedule      = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;adaptive_cfl&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # per-window adaptive substep count</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">substep_cfl_target    = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.95</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">min_steps_per_window  = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">max_steps_per_window  = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">16</span></span></code></pre></div><p>The source descriptor (referenced via <code>[source] toml = …</code>) declares collection mappings and FV3-specific invariants. The actual descriptor TOML is in <code>config/met_sources/geosit.toml</code>; the keys the preprocessor reads sit under <code>[preprocessing]</code>:</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># config/met_sources/geosit.toml (extract — full file is longer)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">preprocessing</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mass_flux_dt_seconds  = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">450.0</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # FV3 dynamics step — see Conventions</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">level_orientation     = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;auto&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # bottom_up | top_down | auto</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">collections_required  = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;CTM_A1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;CTM_I1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">collections_optional  = [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;A3mstE&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;A3dyn&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># used when convection is on</span></span></code></pre></div><p>The preprocessing TOML&#39;s <code>[source]</code> block can override <code>include_surface</code>, <code>include_convection</code>, <code>include_vdiff_fields</code>, and, for GEOS-FP, <code>physics_dir</code> / <code>physics_layout</code>. GEOS-IT reads native A1/A3 files next to CTM_A1/CTM_I1. GEOS-FP reads native C720 CTM files from <code>root_dir</code> and physics fallback files from <code>physics_dir</code>.</p><h2 id="Adaptive-substep-schedule" tabindex="-1">Adaptive substep schedule <a class="header-anchor" href="#Adaptive-substep-schedule" aria-label="Permalink to &quot;Adaptive substep schedule {#Adaptive-substep-schedule}&quot;">​</a></h2><p>The CS GEOS-native path supports per-window adaptive substep counts. When <code>[numerics].substep_schedule = &quot;adaptive_cfl&quot;</code>:</p><ol><li><p><code>_geos_select_steps_for_window!</code> runs up to 8 refinement iterations per window, evaluating the palindrome positivity budget <code>2·(out_x + out_y + out_z) / m_start</code> against <code>substep_cfl_target</code>.</p></li><li><p>The final per-window substep count is collected into a <code>steps_per_window_by_window :: Vector{Int}</code> of length 24.</p></li><li><p><code>driver_before_close_writer!</code> patches the schedule into the streaming binary header via <code>set_streaming_steps_per_window_schedule!</code>.</p></li><li><p>The header carries <code>runtime_substep_contract = &quot;binary_schedule&quot;</code>, which tells the runtime to advance using the per-window schedule rather than a single scalar.</p></li></ol><p>C180 production binaries use this. The scalar <code>steps_per_window</code> in the header equals <code>maximum(schedule)</code>; the runtime reads <code>steps_per_window_by_window</code> instead and gets per-window granularity.</p><h2 id="GCHP-VDIFF-preprocessing-payload" tabindex="-1">GCHP VDIFF preprocessing payload <a class="header-anchor" href="#GCHP-VDIFF-preprocessing-payload" aria-label="Permalink to &quot;GCHP VDIFF preprocessing payload {#GCHP-VDIFF-preprocessing-payload}&quot;">​</a></h2><p>When <code>[source].include_vdiff_fields = true</code>, the preprocessor writes four extra payload sections:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Section</th><th style="text-align:right;">Contents</th></tr></thead><tbody><tr><td style="text-align:right;"><code>:vdiff_u</code>, <code>:vdiff_v</code></td><td style="text-align:right;">Substep-cadence horizontal wind components</td></tr><tr><td style="text-align:right;"><code>:vdiff_t</code>, <code>:vdiff_qv</code></td><td style="text-align:right;">Substep-cadence temperature and specific humidity</td></tr></tbody></table><p>These feed the <code>LocalHoltslagBovilleKzField</code> local Kz at runtime. The binary&#39;s capability surface advertises <code>gchp_vdiff = true</code> once all four are present. The runtime&#39;s <code>[diffusion].kind = &quot;geoschem_holtslag_boville_vdiff&quot;</code> requires this capability.</p><h2 id="Per-window-pipeline" tabindex="-1">Per-window pipeline <a class="header-anchor" href="#Per-window-pipeline" aria-label="Permalink to &quot;Per-window pipeline {#Per-window-pipeline}&quot;">​</a></h2><p>For each of the 24 hourly windows:</p><ol><li><strong>Read</strong> (<code>src/Preprocessing/sources/geos.jl::read_window!</code>):</li></ol><ul><li><p>Open <code>CTM_A1</code> for hourly <code>MFXC</code>, <code>MFYC</code>, <code>DELP</code> (window-constant).</p></li><li><p>Open <code>CTM_I1</code> for instantaneous <code>PS</code>, <code>QV</code> at hour <code>n</code> and <code>n+1</code>.</p></li><li><p>Expose <code>MFXC</code> / <code>MFYC</code> as a rate-like diagnostic by dividing by <code>mass_flux_dt = 450 s</code>. CTM_A1 stores a dry pressure-area transport amount for one 450 s dynamics step, so the GEOS-CS writer stores each Strang half-sweep face flux as <code>MFXC / (2g)</code> and reuses that amount for the 8 substeps in the hourly window. The logged <code>flux_scale</code>is<code>450 / (2g) = 22.94361479</code>.</p></li><li><p>Auto-detect level orientation (<code>detect_level_orientation</code>): GEOS-IT files are <strong>bottom-up</strong> (k=1 surface), the runtime expects <strong>top-down</strong> (k=1 TOA). The reader flips once at read time so all downstream code sees the runtime convention.</p></li></ul><ol start="2"><li><strong>Endpoint dry-mass reconstruction</strong> (<code>endpoint_dry_mass!</code>):</li></ol><ul><li>From <code>PS_total</code> + <code>QV</code> + the hybrid-σ-pressure coefficients, reconstruct <code>DELP_dry</code>, <code>PS_dry</code>, ensuring <code>Σ DELP_dry = PS_dry</code> to machine precision.</li></ul><ol start="3"><li><strong>Native MFXC/MFYC → v4 face flux layout</strong> (<code>geos_native_to_face_flux!</code>):</li></ol><ul><li><p>GEOS-IT MFXC/MFYC are <strong>already on a dry basis</strong> (per the GMAO product manual). No <code>(1 − qv)</code> correction is applied.</p></li><li><p>Re-stagger from the FV3 layout (cell-centered values that represent the east / north face) into the v4 layout (<code>am[i+1, j, k] = MFXC[i, j, k]</code>).</p></li><li><p>Sync west / south halos via <code>_propagate_cs_outflow_to_halo!</code>.</p></li></ul><ol start="4"><li><strong>Raw dry endpoint target + column balance</strong> (<code>balance_cs_column_mass_fluxes!</code>):</li></ol><ul><li><p>The next-hour raw GEOS <code>DELP_dry</code> endpoint is transformed with the same vertical plan as <code>m_cur</code>.</p></li><li><p>Native horizontal fluxes are column-balanced to that endpoint for the selected per-window substep count.</p></li></ul><ol start="5"><li><strong>Diagnosed vertical mass flux</strong> (<code>diagnose_cs_cm!</code>):</li></ol><ul><li><p>The binary stores <code>dm = (m_next_raw - m_cur) / (2 * n_sub[win])</code>.</p></li><li><p><code>cm</code> is diagnosed from the balanced horizontal fluxes and this <code>dm</code>, so runtime replay closes against the same raw endpoint used by the positivity gate.</p></li><li><p>With <code>chain_mass = true</code>, the raw endpoint is chained into the next window/day. With <code>chain_mass = false</code>, every window seeds <code>m_cur</code> from the raw GEOS start endpoint.</p></li></ul><ol start="6"><li><p><strong>Write-time replay gate</strong> — same contract as the spectral path (<code>verify_write_replay_cs!</code>); failures abort.</p></li><li><p><strong>Cross-day chain</strong> — when <code>chain_mass = true</code>, the day&#39;s <code>final_m</code> is threaded as <code>seed_m</code> for the next day&#39;s <code>process_day</code> invocation. When <code>chain_mass = false</code>, no cross-day seed is returned; each day/window is re-seeded from raw GEOS mass.</p></li></ol><h2 id="GCHP-style-convection-wiring" tabindex="-1">GCHP-style convection wiring <a class="header-anchor" href="#GCHP-style-convection-wiring" aria-label="Permalink to &quot;GCHP-style convection wiring {#GCHP-style-convection-wiring}&quot;">​</a></h2><p>When <code>[source] include_convection = true</code>:</p><ol><li><p>The reader opens <code>A3mstE</code> and <code>A3dyn</code> (3-hourly NetCDF). If either collection is missing, the open fails loudly — there is no silent fallback.</p></li><li><p><strong>3-hourly hold-constant binding</strong> — every hourly window <code>w</code> reads from A3 record index <code>(w − 1) ÷ 3 + 1</code>. Windows 1, 2, 3 see the same A3 record; windows 22, 23, 24 see the 8th. The dry-basis correction is still per-window (see step 3).</p></li><li><p><strong>Dry-basis correction</strong> (<code>_moist_to_dry_dtrain!</code>, <code>_moist_to_dry_cmfmc!</code>):</p></li></ol><ul><li><p>GMAO ships <code>CMFMC</code> and <code>DTRAIN</code> as <strong>moist-air</strong> mass fluxes, kg / m² / s. The runtime needs them on the same dry basis as <code>state.air_mass</code>, so the reader multiplies by <code>(1 − qv_face)</code> using the window-mean QV (average of <code>t_n</code> and <code>t_{n+1}</code> humidity).</p></li><li><p><code>CMFMC</code> lives at NZ+1 interfaces; the dry factor at face <code>k</code> is the four-corner mean of QV at the two adjacent centers averaged over the two endpoints. <code>DTRAIN</code> is at centers; the dry factor is the simple two-endpoint mean.</p></li></ul><ol start="4"><li><strong>Binary write</strong> — the <code>:cmfmc</code> and <code>:dtrain</code> payload sections land in the binary; <code>inspect_binary</code> advertises <code>cmfmc_convection = true</code> once they&#39;re present. The runtime&#39;s <code>CMFMCConvection</code> operator picks them up via the <code>ConvectionForcing</code> carrier.</li></ol><p>If <code>include_convection = false</code>, the orchestrator&#39;s per-window NamedTuple omits the <code>:cmfmc</code> / <code>:dtrain</code> keys and the writer no-ops them (the binary&#39;s <code>payload_sections</code> does not list them and runtime convection is automatically gated off).</p><h2 id="Performance-notes" tabindex="-1">Performance notes <a class="header-anchor" href="#Performance-notes" aria-label="Permalink to &quot;Performance notes {#Performance-notes}&quot;">​</a></h2><ul><li><p>A 1-day GEOS-IT C180 → CS C180 F32 preprocess is ~1 minute on a recent workstation; with <code>include_convection = true</code>, add ~10 s per day for the A3 reads. <code>include_vdiff_fields = true</code> adds another ~10 s per day for the substep-cadence VDIFF fields.</p></li><li><p>The column-balance + <code>diagnose_cs_cm!</code> path has <strong>zero global Poisson iteration</strong> — every step is a per-column scan, embarrassingly parallel. There&#39;s no diminishing return from adding cores.</p></li><li><p>Real GEOS-IT data closes write-time replay at machine precision for each written window. Worst replay relative errors on real C180 days land around <code>5e-08</code> in F32.</p></li></ul><h2 id="What&#39;s-next" tabindex="-1">What&#39;s next <a class="header-anchor" href="#What&#39;s-next" aria-label="Permalink to &quot;What&amp;#39;s next {#What&#39;s-next}&quot;">​</a></h2><ul><li><p><a href="./regridding">Regridding</a> — how the conservative weights are built and cached (relevant for cross-topology paths).</p></li><li><p><a href="./conventions">Conventions cheat sheet</a> — <code>mass_flux_dt = 450 s</code>, panel conventions, units cheat sheet.</p></li></ul>`,47)])])}const g=s(n,[["render",o]]);export{u as __pageData,g as default};
