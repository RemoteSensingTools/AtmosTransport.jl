import{_ as t,o,c as a,an as i}from"./chunks/framework.BR9x3YLT.js";const g=JSON.parse('{"title":"Adjoint status","description":"","frontmatter":{},"headers":[],"relativePath":"theory/adjoint_status.md","filePath":"theory/adjoint_status.md","lastUpdated":null}'),r={name:"theory/adjoint_status.md"};function s(d,e,n,c,l,h){return o(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="Adjoint-status" tabindex="-1">Adjoint status <a class="header-anchor" href="#Adjoint-status" aria-label="Permalink to &quot;Adjoint status {#Adjoint-status}&quot;">​</a></h1><p>This page marks the current boundary of the adjoint implementation: what can be run and tested today, and which combinations are deliberately rejected. The LinRood adjoint and TM5-style inversion scaffold are implemented and covered by the core test suite, but real-data parity with TM5-4DVAR remains a validation gap.</p><h2 id="What-is-shipped" tabindex="-1">What is shipped <a class="header-anchor" href="#What-is-shipped" aria-label="Permalink to &quot;What is shipped {#What-is-shipped}&quot;">​</a></h2><h3 id="Forward-operators-unchanged" tabindex="-1">Forward operators (unchanged) <a class="header-anchor" href="#Forward-operators-unchanged" aria-label="Permalink to &quot;Forward operators (unchanged) {#Forward-operators-unchanged}&quot;">​</a></h3><p>The forward transport model — advection (four schemes: <code>UpwindScheme</code>, <code>SlopesScheme</code>, <code>PPMScheme</code>, <code>LinRoodPPMScheme</code>), convection (CMFMC + TM5), implicit vertical diffusion (Beljaars–Viterbo / Holtslag–Boville Kz fields), and surface flux sources — is implemented for the runtime&#39;s supported topology/backend combinations and covered by the test suite documented in <a href="/AtmosTransport.jl/v0.3/theory/conservation_budgets#Conservation-budgets">Conservation budgets</a>.</p><h3 id="Tape,-checkpoint,-and-reverse-pass" tabindex="-1">Tape, checkpoint, and reverse pass <a class="header-anchor" href="#Tape,-checkpoint,-and-reverse-pass" aria-label="Permalink to &quot;Tape, checkpoint, and reverse pass {#Tape,-checkpoint,-and-reverse-pass}&quot;">​</a></h3><p><code>src/Adjoints/</code> ships a full discrete-adjoint pipeline for the cubed-sphere split-sweep path, with a separate kernel-level pipeline for <code>LinRoodPPMScheme</code> integrated through <code>cs_surface_emission_footprint</code>.</p><p>The shipped pieces, all exported through <code>AtmosTransport.Adjoints</code>:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Surface</th><th style="text-align:right;">What it does</th></tr></thead><tbody><tr><td style="text-align:right;"><code>cs_surface_emission_footprint</code></td><td style="text-align:right;">Forward → tape → reverse pass driver for CS, returning <code>dJ/dE_t</code> at every surface step</td></tr><tr><td style="text-align:right;"><code>cs_surface_emission_footprint_from_seed</code></td><td style="text-align:right;">Same, but accepts an explicit final <code>dJ/drm</code> seed for arbitrary observation operators</td></tr><tr><td style="text-align:right;"><code>cs_surface_flux_jacobian</code></td><td style="text-align:right;">Batches layer/column objectives, aggregates per-step footprints into named user windows</td></tr><tr><td style="text-align:right;"><code>cs_surface_flux_4dvar</code></td><td style="text-align:right;">4D-Var cost / gradient with named controls, step-indexed scalar observations, diagonal background</td></tr><tr><td style="text-align:right;"><code>cs_surface_flux_4dvar_optimize</code></td><td style="text-align:right;">Dependency-free gradient-descent driver for the above</td></tr><tr><td style="text-align:right;"><code>cs_surface_flux_4dvar_solve</code></td><td style="text-align:right;">Production driver with optimizer choice (<code>CSGradientDescent</code> / <code>CSLBFGS</code>)</td></tr></tbody></table><p>The adjoint supports the following advection schemes (full union in <code>CSAdjointSupportedScheme</code>):</p><ul><li><p><code>UpwindScheme()</code></p></li><li><p><code>SlopesScheme(NoLimiter())</code></p></li><li><p><code>PPMScheme(NoLimiter())</code></p></li><li><p><code>PPMScheme(MonotoneLimiter())</code> — via a stored tracer-branch tape around the base trajectory</p></li><li><p><code>LinRoodPPMScheme(; ppm_order = 5)</code></p></li><li><p><code>LinRoodPPMScheme(; ppm_order = 7)</code></p></li></ul><p>The supporting kernel adjoints in <code>src/Operators/Advection/linrood_adjoint_kernels.jl</code> are transposition-tested (<code>test/core/test_linrood_kernel_adjoints.jl</code>) and finite-difference VJP-tested via single-panel and cross-panel halo compositions.</p><p>The CS reverse pass also supports the default, unclamped/full-column/unmerged forms of <code>TM5Convection</code>, <code>CMFMCConvection</code>, and <code>CMFMCMatrixConvection</code>. CMFMC and CMFMC-matrix have column adjoint-identity tests; the TM5/CMFMC footprint paths are checked against finite-difference emission gradients in <code>test_cs_ppm_adjoint_footprint.jl</code>.</p><h3 id="Checkpoint-schedules" tabindex="-1">Checkpoint schedules <a class="header-anchor" href="#Checkpoint-schedules" aria-label="Permalink to &quot;Checkpoint schedules {#Checkpoint-schedules}&quot;">​</a></h3><p><code>src/Tape/CheckpointSchedule.jl</code> ships three checkpoint policies, selectable via the <code>checkpoint</code> kwarg of <code>cs_surface_emission_footprint</code>:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Policy</th><th style="text-align:right;">Memory</th><th style="text-align:right;">Recompute</th></tr></thead><tbody><tr><td style="text-align:right;"><code>FullCheckpoint</code></td><td style="text-align:right;">O(N) state snapshots</td><td style="text-align:right;">none — fastest reverse pass</td></tr><tr><td style="text-align:right;"><code>StrideCheckpoint(stride)</code></td><td style="text-align:right;">O(N/stride)</td><td style="text-align:right;">replays each stride forward</td></tr><tr><td style="text-align:right;"><code>RevolveCheckpoint()</code></td><td style="text-align:right;">O(log N)</td><td style="text-align:right;">recursive bisection; each step may be replayed once per recursion level</td></tr></tbody></table><p><code>RevolveCheckpoint()</code> has no snapshot-budget argument. It is a recursive bisection variant rather than the optimal binomial Revolve: peak snapshots are proportional to the recursion depth and total replay work is <code>O(N log N)</code>. It is covered in <code>test/core/test_cs_stride_checkpoint.jl</code>.</p><h3 id="Tape-storage-backends" tabindex="-1">Tape storage backends <a class="header-anchor" href="#Tape-storage-backends" aria-label="Permalink to &quot;Tape storage backends {#Tape-storage-backends}&quot;">​</a></h3><p>Split-sweep tape records can live on the device (the default), in pinned-host memory, or on disk via mmap — selectable via the <code>tape_storage</code> kwarg (<code>:device</code> / <code>:pinned_host</code> / <code>:mmap</code>). Storage backends are defined in <code>src/Tape/TapeStorage.jl</code> and <code>src/Tape/MmapTapeStorage.jl</code>; the mmap path is covered by <code>test_cs_tape_mmap_roundtrip.jl</code>. LinRood tape records currently require <code>:device</code> storage.</p><h3 id="Inversion-scaffold" tabindex="-1">Inversion scaffold <a class="header-anchor" href="#Inversion-scaffold" aria-label="Permalink to &quot;Inversion scaffold {#Inversion-scaffold}&quot;">​</a></h3><p><code>src/Inversion/</code> ships:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Module</th><th style="text-align:right;">Surface</th></tr></thead><tbody><tr><td style="text-align:right;"><code>Covariance.jl</code></td><td style="text-align:right;"><code>DiagonalCSCovariance</code>, <code>IsotropicGaussianCSCovariance</code>, <code>apply_B_half!</code>, <code>apply_B_half_adjoint!</code>, <code>apply_B_half_inverse!</code></td></tr><tr><td style="text-align:right;"><code>Preconditioning.jl</code></td><td style="text-align:right;"><code>apply_preconditioner!</code>, <code>LinearOptimType</code>, <code>LogNormalOptimType</code></td></tr><tr><td style="text-align:right;"><code>Optimizer.jl</code></td><td style="text-align:right;"><code>AbstractCSOptimizer</code>, <code>CSGradientDescent</code>, <code>CSLBFGS</code> (via multiple-dispatch surfaces)</td></tr><tr><td style="text-align:right;"><code>Jacobian.jl</code></td><td style="text-align:right;">Footprint-Jacobian assembly for batched observation operators</td></tr><tr><td style="text-align:right;"><code>CostGradient.jl</code></td><td style="text-align:right;">4D-Var cost / gradient evaluator with preconditioning</td></tr><tr><td style="text-align:right;"><code>Observations.jl</code>, <code>ObservationBinding.jl</code>, <code>ObservationsIO.jl</code></td><td style="text-align:right;">Typed observation surface + on-disk schema</td></tr><tr><td style="text-align:right;"><code>DeparturesIO.jl</code></td><td style="text-align:right;">Departures-file IO</td></tr></tbody></table><p>These are all on CI. Full inversion tests: <code>test_cs_4dvar_preconditioned.jl</code>, <code>test_cs_lbfgs.jl</code>, <code>test_cs_inversion_driver.jl</code>, <code>test_cs_inversion_truth_recovery.jl</code>, <code>test_cs_iteration_log.jl</code>, <code>test_cs_observations_io.jl</code>, <code>test_cs_observation_binding.jl</code>, <code>test_cs_departures_io.jl</code>, <code>test_cs_covariance.jl</code>, <code>test_cs_preconditioning.jl</code>, <code>test_cs_optimizer_dispatch.jl</code>.</p><h2 id="What-is-not-yet-shipped" tabindex="-1">What is not yet shipped <a class="header-anchor" href="#What-is-not-yet-shipped" aria-label="Permalink to &quot;What is not yet shipped {#What-is-not-yet-shipped}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Item</th><th style="text-align:right;">Notes</th></tr></thead><tbody><tr><td style="text-align:right;"><strong>Optimal binomial Revolve</strong></td><td style="text-align:right;"><code>RevolveCheckpoint</code> ships as the bisection variant — logarithmic memory but not the Griewank–Walther optimal recompute count. Optimal binomial Revolve is the next refinement.</td></tr><tr><td style="text-align:right;"><strong>Optimized/clamped convection adjoints</strong></td><td style="text-align:right;">CMFMC <code>clamp = true</code> and TM5/CMFMC-matrix collaborative, truncated, or merged solves are rejected by the footprint API until their exact branches are taped and transposed.</td></tr><tr><td style="text-align:right;"><strong>TM5-4DVAR cross-validation</strong></td><td style="text-align:right;">Synthetic truth-recovery via <code>test_cs_inversion_truth_recovery.jl</code> is on CI; a side-by-side parity run against TM5-4DVAR on real data has not been published.</td></tr><tr><td style="text-align:right;"><strong>Tangent-linear model</strong></td><td style="text-align:right;">Forward TL is not exposed as a separate driver. If you need it, use the reverse pass plus identity seeding.</td></tr></tbody></table><h2 id="Adjoint-readiness-in-the-forward-design" tabindex="-1">Adjoint-readiness in the forward design <a class="header-anchor" href="#Adjoint-readiness-in-the-forward-design" aria-label="Permalink to &quot;Adjoint-readiness in the forward design {#Adjoint-readiness-in-the-forward-design}&quot;">​</a></h2><p>Three concrete forward-design choices that pay off in the adjoint:</p><ul><li><p><strong>Vertical diffusion</strong> — the Thomas-tridiagonal coefficients <code>(a, b, c)</code> are kept as <strong>named locals at every level <code>k</code></strong> rather than fused into a pre-factored <code>(b, factor)</code> form. The Diffusion module docstring records this as a deliberate adjoint-readiness choice. The CS reverse pass uses that layout to transpose the Backward-Euler column solve, including the tracer-mass / VMR scaling.</p></li><li><p><strong>Convection (CMFMC + TM5)</strong> — <code>apply!</code> takes a <code>ConvectionForcing</code> carrier explicitly so the operator does not call <code>current_time</code> internally; this keeps the operator pure-functional in the time variable. The TM5 reverse pass rebuilds the same per-column matrix and solves with the transposed LU factors.</p></li><li><p><strong>Advection</strong> — the Strang palindrome&#39;s time symmetry means the forward integrator is its own time-reverse; the adjoint of the composition is the composition of the adjoints in reverse order, which is structurally the same code path with each operator&#39;s adjoint substituted in.</p></li></ul><h2 id="How-to-use-the-adjoint-today" tabindex="-1">How to use the adjoint today <a class="header-anchor" href="#How-to-use-the-adjoint-today" aria-label="Permalink to &quot;How to use the adjoint today {#How-to-use-the-adjoint-today}&quot;">​</a></h2><p>The maintained command-line path is the inversion driver:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=.</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> scripts/inversions/cs_4dvar.jl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">    config/inversions/example_synthetic.toml</span></span></code></pre></div><p>That configuration owns observations, controls, covariance, optimizer, and checkpoint choices. For the lower-level Julia API, start with <code>test/core/test_cs_inversion_truth_recovery.jl</code>; it constructs the panel arrays, step sequences, mesh, and objective required by <code>cs_surface_emission_footprint</code>. Use <code>RevolveCheckpoint()</code> (without arguments) for recursive bisection, or <code>StrideCheckpoint(K)</code> for an explicit interval. LinRood checkpoints currently require <code>tape_storage = :device</code>; split-sweep schemes also support <code>:pinned_host</code> and <code>:mmap</code>.</p><h2 id="Where-to-read-next" tabindex="-1">Where to read next <a class="header-anchor" href="#Where-to-read-next" aria-label="Permalink to &quot;Where to read next {#Where-to-read-next}&quot;">​</a></h2><ul><li><p><a href="/AtmosTransport.jl/v0.3/theory/validation_status#Validation-status">Validation status</a> — what the forward model has been validated against.</p></li><li><p><a href="/AtmosTransport.jl/v0.3/theory/conservation_budgets#Conservation-budgets">Conservation budgets</a> — the explicit verification tests the forward operators pass.</p></li><li><p><a href="./../for_tm5_gchp_users/adjoints">Adjoints on top of the binary</a> — TM5-4DVAR / GIGC-adjoint user perspective on how the pipeline maps to those workflows.</p></li></ul>`,34)])])}const u=t(r,[["render",s]]);export{g as __pageData,u as default};
