import{_ as e,o,c as r,an as s}from"./chunks/framework.C1Zpw0WN.js";const p=JSON.parse('{"title":"Conservation budgets","description":"","frontmatter":{},"headers":[],"relativePath":"theory/conservation_budgets.md","filePath":"theory/conservation_budgets.md","lastUpdated":null}'),a={name:"theory/conservation_budgets.md"};function i(n,t,d,c,l,h){return o(),r("div",null,[...t[0]||(t[0]=[s('<h1 id="Conservation-budgets" tabindex="-1">Conservation budgets <a class="header-anchor" href="#Conservation-budgets" aria-label="Permalink to &quot;Conservation budgets {#Conservation-budgets}&quot;">​</a></h1><p>The conservation contract from <a href="/AtmosTransport.jl/stable/theory/mass_conservation#Mass-conservation">Mass conservation</a> is enforced by <strong>explicit <code>@test</code> assertions</strong> in the test suite. This page lists the tests that anchor each conservation property, with file:line citations and the exact tolerance bounds so a reader can verify by inspection.</p><p>All tolerance values quoted here come straight from the test source; none of them are bit-exact. Bit-exact conservation isn&#39;t achievable in floating-point — what the tests assert is closure within machine-precision noise floors that scale with <code>FT</code>.</p><h2 id="Uniform-tracer-preservation" tabindex="-1">Uniform tracer preservation <a class="header-anchor" href="#Uniform-tracer-preservation" aria-label="Permalink to &quot;Uniform tracer preservation {#Uniform-tracer-preservation}&quot;">​</a></h2><p>The simplest non-trivial conservation property: a tracer initialised to a uniform constant value <code>χ_0</code> everywhere should stay close to <code>χ_0</code> after multi-step advection on a non-trivial flow.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Test (<code>test/core/test_advection_kernels.jl</code>)</th><th style="text-align:right;">Asserts</th><th style="text-align:right;">Tolerance</th></tr></thead><tbody><tr><td style="text-align:right;">CPU uniform-invariance test sets</td><td style="text-align:right;"><code>maximum(abs(χ_out − 4.0e-4)) / 4.0e-4 &lt; 1e-6</code> after one Strang step on a sinusoidal flow. CPU coverage spans <code>Upwind</code> / <code>Slopes</code> / <code>PPM</code>.</td><td style="text-align:right;"><code>&lt; 1e-6</code> (both F32 and F64)</td></tr><tr><td style="text-align:right;">GPU uniform-invariance test set</td><td style="text-align:right;">same assertion on <code>CuArray</code>-backed state — currently for <strong><code>Upwind</code></strong>. The Slopes / PPM uniform-invariance cases are CPU-only.</td><td style="text-align:right;"><code>&lt; 1e-6</code></td></tr></tbody></table><p>The test fixture builds a synthetic LL <code>36 × 18 × 4</code> setup together with a sinusoidal zonal <code>am</code> tapered with latitude and a <code>cm</code> diagnosed from continuity to keep the synthetic forcing self-consistent. CFL ≈ 0.15 at the equator. Schemes covered: <code>UpwindScheme</code>, <code>SlopesScheme</code>, <code>PPMScheme</code> — each in its own <code>@testset</code> block. CPU coverage spans all three; <strong>GPU uniform-invariance coverage is <code>Upwind</code> only</strong>. The Slopes / PPM uniform tests run on CPU only.</p><h2 id="Mass-budget-conservation" tabindex="-1">Mass-budget conservation <a class="header-anchor" href="#Mass-budget-conservation" aria-label="Permalink to &quot;Mass-budget conservation {#Mass-budget-conservation}&quot;">​</a></h2><p>Same kernel, but on a non-uniform tracer (a meridional gradient <code>rm_grad_cpu</code>):</p><table tabindex="0"><thead><tr><th style="text-align:right;">Test (<code>test/core/test_advection_kernels.jl</code>)</th><th style="text-align:right;">Asserts</th><th style="text-align:right;">Tolerance</th></tr></thead><tbody><tr><td style="text-align:right;">CPU mass conservation, uniform</td><td style="text-align:right;"><code>abs(Σ m_out − Σ m_cpu) / Σ m_cpu &lt; tol</code> and the same for tracer mass</td><td style="text-align:right;">F64: <code>&lt; 1e-13</code>. F32: <code>&lt; 1e-5</code></td></tr><tr><td style="text-align:right;">CPU mass conservation, gradient over 4 steps</td><td style="text-align:right;">same assertion after 4 Strang steps with a gradient IC</td><td style="text-align:right;">F64: <code>&lt; 1e-12</code>. F32: <code>&lt; 5e-5</code></td></tr><tr><td style="text-align:right;">GPU mass conservation, gradient over 4 steps</td><td style="text-align:right;">same on a <code>CuArray</code>-backed state</td><td style="text-align:right;">F64: <code>&lt; 1e-12</code>. F32: <code>&lt; 5e-5</code></td></tr><tr><td style="text-align:right;">CPU non-trivial transport</td><td style="text-align:right;"><code>maximum(abs(rm_out − rm_grad_cpu)) &gt; 0</code> — sanity that the run actually moved tracer mass (not just held it constant)</td><td style="text-align:right;">strict <code>&gt;</code></td></tr></tbody></table><p><code>test/core/test_cubed_sphere_advection.jl</code> covers the PPM-on-CS variants of the same suite, exercising the panel-edge halo sync end to end.</p><h2 id="CPU-/-GPU-agreement" tabindex="-1">CPU / GPU agreement <a class="header-anchor" href="#CPU-/-GPU-agreement" aria-label="Permalink to &quot;CPU / GPU agreement {#CPU-/-GPU-agreement}&quot;">​</a></h2><p>Bit-exact CPU/GPU agreement is <strong>not</strong> asserted: floating-point associativity differs between sequential CPU sums and parallel GPU reductions, and on F64 the GPU&#39;s FMA instruction can produce a small ULP-scale difference per multiply-add. The test suite uses ULP-bounded tolerances that vary by scheme and step count:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Test (<code>test/core/test_advection_kernels.jl</code>)</th><th style="text-align:right;">Tolerance</th></tr></thead><tbody><tr><td style="text-align:right;">Upwind, 1 step</td><td style="text-align:right;"><code>4 * eps(FT)</code> per cell</td></tr><tr><td style="text-align:right;">Upwind, 4 steps</td><td style="text-align:right;"><code>16 * eps(FT)</code> per cell</td></tr><tr><td style="text-align:right;">Slopes, 4 steps</td><td style="text-align:right;"><code>16 * eps(FT)</code> per cell</td></tr><tr><td style="text-align:right;">PPM, 4 steps</td><td style="text-align:right;"><code>16 * eps(FT)</code> per cell</td></tr></tbody></table><p><code>LinRoodPPMScheme</code> is not in this CPU/GPU agreement matrix; it has a forward runtime coverage in the opt-in orphan test <code>test/orphan/test_cubed_sphere_runtime.jl</code>, but no per-step GPU comparison.</p><p>The CPU/GPU agreement check runs ONLY when CUDA.jl is loaded; it&#39;s gated by <code>HAS_GPU</code> at the top of the test file. CI runs the CPU side; GPU coverage is exercised on machines with hardware.</p><h2 id="Cross-window-mass-closure-replay-gate" tabindex="-1">Cross-window mass closure (replay gate) <a class="header-anchor" href="#Cross-window-mass-closure-replay-gate" aria-label="Permalink to &quot;Cross-window mass closure (replay gate) {#Cross-window-mass-closure-replay-gate}&quot;">​</a></h2><p>Per-window replay is the contract that lets the runtime stream window N+1 starting from window N&#39;s evolved endpoint without drift. Tested in <code>test/core/test_replay_consistency.jl</code>:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Subtest (line)</th><th style="text-align:right;">Asserts</th></tr></thead><tbody><tr><td style="text-align:right;"><code>verify_window_continuity_ll</code> with continuity-consistent data</td><td style="text-align:right;"><code>tol_rel ≤ 1e-12</code> (F64) / <code>1e-6</code> (F32). Both pass on the synthetic fixture.</td></tr><tr><td style="text-align:right;">Deliberately broken <code>cm</code> storage</td><td style="text-align:right;">The storage-level gate fires <code>@test_throws ErrorException</code> when the binary&#39;s stored <code>cm</code> violates the explicit-<code>dm</code> closure by more than <code>replay_tolerance(FT)</code>. A separate subtest checks the diagnostic residual against the same broken field.</td></tr><tr><td style="text-align:right;">Final-window inconsistent <code>cm</code></td><td style="text-align:right;">The LL storage replay-gate test deliberately injects an inconsistent final-window <code>cm</code> and asserts <code>@test_throws ErrorException</code> — i.e. the gate <strong>does</strong> detect inconsistency at the day boundary, not silently pass it as zero-tendency.</td></tr></tbody></table><p>The same gate runs at preprocessing write time (always) and at runtime load time (opt-in via <code>[met_data] validate_replay = true</code> or <code>ATMOSTR_REPLAY_CHECK = 1</code>). The test exercises the function directly so the contract is validated independently of the preprocessor / runtime drivers.</p><h2 id="Cross-topology-regridding-conservation" tabindex="-1">Cross-topology (regridding) conservation <a class="header-anchor" href="#Cross-topology-regridding-conservation" aria-label="Permalink to &quot;Cross-topology (regridding) conservation {#Cross-topology-regridding-conservation}&quot;">​</a></h2><p>When mass moves between topologies (LL → CS in the spectral preprocessor; LL → RG in the LL→RG variant), the conservative regridder preserves total mass exactly but may shift the per-level distribution by <code>O(10⁻⁶)</code>.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Test</th><th style="text-align:right;">Asserts</th></tr></thead><tbody><tr><td style="text-align:right;"><code>test/regridding/test_conservation.jl</code></td><td style="text-align:right;"><code>sum(m_dest) ≈ sum(m_source)</code> under LL↔CS conservative regrid.</td></tr><tr><td style="text-align:right;"><code>test/core/test_ll_to_cs_regrid_script.jl</code></td><td style="text-align:right;">End-to-end LL → CS regrid pipeline produces a binary whose stored <code>m</code>, summed globally, matches the LL source&#39;s stored <code>m</code> to <code>1e-6</code> relative — the script-level acceptance gate, deliberately looser than the kernel-level <code>1e-13</code> to absorb the per-level mass-consistency redistribution.</td></tr></tbody></table><p>The per-level mass-consistency correction in <code>cs_transport_helpers.jl::_enforce_perlevel_mass_consistency!</code> is what closes the per-level distribution; tested implicitly via the Poisson balance convergence requirement (would not converge to the current dry-basis replay tolerance without the correction).</p><h2 id="Initial-condition-mass-conservation" tabindex="-1">Initial-condition mass conservation <a class="header-anchor" href="#Initial-condition-mass-conservation" aria-label="Permalink to &quot;Initial-condition mass conservation {#Initial-condition-mass-conservation}&quot;">​</a></h2><p>When a uniform-VMR initial condition is constructed, the conversion <code>χ × air_mass</code> should yield a tracer-mass field whose ratio to <code>air_mass</code> is <code>χ</code> everywhere. Trivially true for a uniform IC; matters when the IC interpolates from a different mesh.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Test</th><th style="text-align:right;">Asserts</th></tr></thead><tbody><tr><td style="text-align:right;"><code>test/core/test_initial_condition_io.jl</code></td><td style="text-align:right;">The <code>file</code> / <code>netcdf</code> IC kinds round-trip a known field through the IC pipeline and assert the recovered mixing ratio matches the source within tolerance.</td></tr><tr><td style="text-align:right;"><code>test/core/test_basis_explicit_core.jl</code></td><td style="text-align:right;">Dry-basis IC interpretation: <code>[tracers.co2.init] kind = &quot;uniform&quot;; background = 4.0e-4</code> produces a tracer field whose <code>mixing_ratio(state, :CO2)</code> is <code>4.0e-4</code> exactly when <code>air_mass</code> is on dry basis.</td></tr></tbody></table><h2 id="Preprocessor-contract-suite" tabindex="-1">Preprocessor contract suite <a class="header-anchor" href="#Preprocessor-contract-suite" aria-label="Permalink to &quot;Preprocessor contract suite {#Preprocessor-contract-suite}&quot;">​</a></h2><p>The unified preprocessor (<code>run_unified_preprocessor_day!</code>) anchors its window-contract invariants with a dedicated test set:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Test</th><th style="text-align:right;">Asserts</th></tr></thead><tbody><tr><td style="text-align:right;"><code>test_ll_preprocessor_contract.jl</code></td><td style="text-align:right;"><code>LatLonContract</code> + <code>LatLonSpectralWindowWorkspace</code> + LL writer satisfy mass-balance and per-substep positivity gates.</td></tr><tr><td style="text-align:right;"><code>test_rg_preprocessor_contract.jl</code></td><td style="text-align:right;">Same on the face-indexed RG topology, including the boundary-stub-flux invariant for pole-singular faces.</td></tr><tr><td style="text-align:right;"><code>test_cs_preprocessor_contract.jl</code></td><td style="text-align:right;">Same on cubed-sphere, including the palindrome-budget positivity check.</td></tr><tr><td style="text-align:right;"><code>test_preprocessor_unified_driver.jl</code></td><td style="text-align:right;">The driver dispatches correctly across (source × vertical × target).</td></tr><tr><td style="text-align:right;"><code>test_preprocessor_writer_adapters.jl</code></td><td style="text-align:right;">Each <code>AbstractBinaryWriter</code> matches its paired <code>AbstractWindowContract</code> on basis and topology at the type level.</td></tr><tr><td style="text-align:right;"><code>test_{ll,rg,cs}_spectral_unified_driver.jl</code></td><td style="text-align:right;">End-to-end day builds for the spectral path.</td></tr></tbody></table><h2 id="Inversion-/-adjoint-suite" tabindex="-1">Inversion / adjoint suite <a class="header-anchor" href="#Inversion-/-adjoint-suite" aria-label="Permalink to &quot;Inversion / adjoint suite {#Inversion-/-adjoint-suite}&quot;">​</a></h2><p>The CS surface-emission footprint and 4D-Var stack are anchored by:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Test</th><th style="text-align:right;">Asserts</th></tr></thead><tbody><tr><td style="text-align:right;"><code>test_cs_ppm_adjoint_footprint.jl</code></td><td style="text-align:right;">Kernel-level transposition + finite-difference VJPs for split-sweep schemes.</td></tr><tr><td style="text-align:right;"><code>test_linrood_kernel_adjoints.jl</code></td><td style="text-align:right;">Kernel-level transposition for the LinRood adjoint kernels (ORD=5 and ORD=7).</td></tr><tr><td style="text-align:right;"><code>test_cs_inversion_truth_recovery.jl</code></td><td style="text-align:right;">Synthetic-truth-recovery end-to-end through <code>cs_surface_flux_4dvar_solve</code> + L-BFGS.</td></tr><tr><td style="text-align:right;"><code>test_cs_4dvar_preconditioned.jl</code>, <code>test_cs_lbfgs.jl</code>, <code>test_cs_optimizer_dispatch.jl</code></td><td style="text-align:right;">Preconditioning, optimizer dispatch, and gradient identities.</td></tr><tr><td style="text-align:right;"><code>test_cs_covariance.jl</code>, <code>test_cs_preconditioning.jl</code></td><td style="text-align:right;"><code>apply_B_half!</code> / <code>apply_B_half_adjoint!</code> / <code>apply_B_half_inverse!</code> identities.</td></tr><tr><td style="text-align:right;"><code>test_cs_observations_io.jl</code>, <code>test_cs_observation_binding.jl</code>, <code>test_cs_departures_io.jl</code></td><td style="text-align:right;">Observation IO + bind-to-mesh + departures-file round-trip.</td></tr><tr><td style="text-align:right;"><code>test_cs_stride_checkpoint.jl</code>, <code>test_cs_tape_mmap_roundtrip.jl</code></td><td style="text-align:right;">Tape storage and checkpoint scheduler correctness, including <code>RevolveCheckpoint</code> cases.</td></tr></tbody></table><h2 id="Test-pass-status" tabindex="-1">Test-pass status <a class="header-anchor" href="#Test-pass-status" aria-label="Permalink to &quot;Test-pass status {#Test-pass-status}&quot;">​</a></h2><p>The <code>test/runtests.jl</code> entry point discovers more than 100 files under <code>test/core/</code>; they run without external meteorology. CI runs that tier and the regridding tier on every push and PR. Real-data tests are opt-in via <code>--real-data</code> or <code>--all</code> and require locally staged inputs.</p><p>Total core-suite case count is in the thousands; CI reports per-test pass / fail breakdown on every run.</p><h2 id="What&#39;s-next" tabindex="-1">What&#39;s next <a class="header-anchor" href="#What&#39;s-next" aria-label="Permalink to &quot;What&amp;#39;s next {#What&#39;s-next}&quot;">​</a></h2><ul><li><p><a href="/AtmosTransport.jl/stable/theory/validation_status#Validation-status">Validation status</a> — what we&#39;ve validated end-to-end against external reference data, vs what&#39;s still synthetic-fixture-only.</p></li><li><p><a href="/AtmosTransport.jl/stable/theory/adjoint_status#Adjoint-status">Adjoint status</a> — the verified support boundary and remaining gaps.</p></li></ul>',38)])])}const u=e(a,[["render",i]]);export{p as __pageData,u as default};
