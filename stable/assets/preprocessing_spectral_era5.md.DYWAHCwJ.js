import{_ as s,o as t,c as a,an as i}from"./chunks/framework.C1Zpw0WN.js";const g=JSON.parse('{"title":"ERA5 spectral path","description":"","frontmatter":{},"headers":[],"relativePath":"preprocessing/spectral_era5.md","filePath":"preprocessing/spectral_era5.md","lastUpdated":null}'),n={name:"preprocessing/spectral_era5.md"};function r(o,e,l,d,p,c){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="ERA5-spectral-path" tabindex="-1">ERA5 spectral path <a class="header-anchor" href="#ERA5-spectral-path" aria-label="Permalink to &quot;ERA5 spectral path {#ERA5-spectral-path}&quot;">​</a></h1><p>The ERA5 spectral path takes ECMWF model-level <strong>vorticity (VO)</strong>, <strong>divergence (D)</strong>, and <strong>log surface pressure (LNSP)</strong> GRIB fields, synthesizes mass-conserving wind / mass-flux fields on a target grid via Holton-style continuity-consistent reconstruction, and writes a transport binary (<code>format_version = 4</code>).</p><p>All three target topologies (LL, RG, CS) run through the unified driver <code>run_unified_preprocessor_day!</code> with the source axis fixed to <code>ERA5SpectralReader</code> and the target axis chosen via <code>[grid].type</code>.</p><h2 id="Required-input" tabindex="-1">Required input <a class="header-anchor" href="#Required-input" aria-label="Permalink to &quot;Required input {#Required-input}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">File pattern</th><th style="text-align:right;">Contents</th><th style="text-align:right;">Hourly?</th></tr></thead><tbody><tr><td style="text-align:right;"><code>era5_spectral_YYYYMMDD_lnsp.gb</code></td><td style="text-align:right;">log(surface pressure) spectral coefficients</td><td style="text-align:right;">yes</td></tr><tr><td style="text-align:right;"><code>era5_spectral_YYYYMMDD_vo_d.gb</code></td><td style="text-align:right;">vorticity + divergence spectral coefficients (137 model levels)</td><td style="text-align:right;">yes</td></tr><tr><td style="text-align:right;"><code>era5_thermo_ml_YYYYMMDD.nc</code></td><td style="text-align:right;">specific humidity (for dry-basis conversion + mass-fix)</td><td style="text-align:right;">yes</td></tr></tbody></table><p>The thermo file is mandatory if <code>[output] mass_basis = &quot;dry&quot;</code> (the default and only currently-tested binary basis for runtime consumption); without it, the dry-basis correction <code>(1 − qv)</code> cannot be applied. ECMWF distributes spectral data via CDS / MARS; the unified downloader is <code>scripts/downloads/download_data.jl</code>, with ERA5 recipes under <code>config/downloads/</code>.</p><p>The preprocessor <strong>prefers</strong> one extra day past the requested range so the last window of each day can read its forward-flux endpoints from the next day&#39;s hour-0 instantaneous fields. <strong>All three target paths fall back to a zero-tendency final-window closure</strong> when the next-day file is missing — the binary still builds, and replay continuity holds, but the very last window of the last day uses an implicit <code>dm = 0</code> rather than a real next-day delta. As a rule of thumb, download <code>[start, end+1]</code> for production runs; a one-day development job can use the fallback if its final-window tendency is not being analyzed.</p><h2 id="Targets" tabindex="-1">Targets <a class="header-anchor" href="#Targets" aria-label="Permalink to &quot;Targets {#Targets}&quot;">​</a></h2><p>The spectral source supports all three target topologies; pick the one that matches your downstream run:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Target</th><th style="text-align:right;">Topology-specific workspace</th></tr></thead><tbody><tr><td style="text-align:right;"><code>LatLonTargetGeometry</code></td><td style="text-align:right;"><code>LatLonSpectralWindowWorkspace</code> (<code>src/Preprocessing/transport_binary/latlon_spectral.jl</code>)</td></tr><tr><td style="text-align:right;"><code>ReducedGaussianTargetGeometry</code></td><td style="text-align:right;"><code>ReducedGaussianSpectralWindowWorkspace</code> (<code>src/Preprocessing/reduced_transport_helpers.jl</code>)</td></tr><tr><td style="text-align:right;"><code>CubedSphereTargetGeometry</code></td><td style="text-align:right;"><code>CubedSphereSpectralWindowWorkspace</code> (<code>src/Preprocessing/transport_binary/cubed_sphere_spectral.jl</code>)</td></tr></tbody></table><p>All three workspaces subtype <code>AbstractWindowWorkspace{G, FT}</code> and are allocated once per day by <code>allocate_window_workspace</code>. The <code>PreprocessorRunCache</code> (allocated once per run) holds the shared expensive objects: the LL→CS conservative regridder, the RG compressed-Laplacian factorization. These persist across days within a single batch invocation.</p><p>The CS target adds a <strong>lat-lon staging step</strong> between spectral synthesis and CS panels — spectral-to-CS-direct is mathematically possible but the conservative regrid via an intermediate LL grid is cheaper and matches the LL/RG paths&#39; precision. The staging resolution is configurable via <code>staging_nlon</code>, <code>staging_nlat</code> in the <code>[grid]</code> block; the defaults <code>max(4·Nc, 360)</code> × <code>max(2·Nc + 1, 181)</code> are what the LL bundle binaries use.</p><h2 id="Per-window-pipeline" tabindex="-1">Per-window pipeline <a class="header-anchor" href="#Per-window-pipeline" aria-label="Permalink to &quot;Per-window pipeline {#Per-window-pipeline}&quot;">​</a></h2><p>For each of the day&#39;s 24 hourly windows:</p><ol><li><strong>Spectral synthesis</strong> (<code>spectral_synthesis.jl::spectral_to_native_fields!</code>)</li></ol><ul><li><p>Reconstruct LNSP → PS via inverse spectral transform.</p></li><li><p>Reconstruct VO + D → (u_stag, v_stag) via the standard IFS streamfunction / velocity-potential decomposition, then to mass fluxes (am, bm) directly from the Holton continuity formula.</p></li><li><p>137 native model levels are synthesized; the <code>level_top</code> / <code>level_bot</code> config window picks a slab.</p></li></ul><ol start="2"><li><strong>Vertical level merging</strong> (<code>vertical_coordinates.jl::merge_thin_levels</code>, <code>select_levels_echlevs</code>)</li></ol><ul><li><p>The <code>[grid] echlevs</code> mode (e.g. <code>&quot;ml137_tropo34&quot;</code>) selects a vertical-level recipe — typically a tropospheric subset merged to ~34 layers via the TM5 r1112 echlevs scheme.</p></li><li><p><code>merge_min_thickness_Pa = 1000.0</code> floors the merged layer thickness so no merged layer is thinner than ~1 km in the tropopause region.</p></li></ul><ol start="3"><li><strong>Mass-fix</strong> (<code>mass_support.jl::pin_global_mean_ps_using_qv!</code>)</li></ol><ul><li><p>Optional, gated by <code>[mass_fix] enable = true</code>.</p></li><li><p>Computes a <strong>uniform PS offset per window</strong> so the global-mean dry surface pressure equals <code>target_ps_dry_pa</code>.</p><ul><li><p>LL and CS targets use the <strong>hourly QV field</strong> for the offset.</p></li><li><p>RG targets use the constant <strong><code>qv_global_climatology</code></strong> scalar (the per-cell hourly QV is dry-converted later in the pipeline; the mass-fix step itself uses the climatology for cost reasons).</p></li></ul></li><li><p>The preprocessor <strong>applies the offset in place</strong> while building the window — the binary&#39;s stored PS, mass, and fluxes are already pinned. The <strong>LL writer</strong> additionally records the per-window offsets in the binary header as <code>ps_offsets_pa_per_window</code> for diagnostic traceability; the <strong>CS and RG writers</strong> carry only <code>mass_fix_enabled</code> (and a handful of related metadata flags), no per-window offset vector. The runtime never re-applies the offsets in any case — there is nothing left to apply.</p></li><li><p>Without <code>[mass_fix]</code>, raw ERA5 spectral analysis drifts by tens of Pa per window, which compounds to noticeable day-boundary tracer-mass jumps over a multi-week run.</p></li></ul><ol start="4"><li><strong>Dry-basis conversion</strong> (<code>mass_support.jl::apply_dry_basis_native!</code>)</li></ol><ul><li><p>Multiplies cell-centered DELP and PS by <code>(1 − qv)</code>; multiplies face-staggered mass fluxes by face-averaged <code>(1 − qv_face)</code>.</p></li><li><p>Applied <strong>before</strong> the binary write; the binary is dry-basis end-to-end.</p></li></ul><ol start="5"><li><strong>Poisson balance</strong> — applied per target, with target-specific solver and finalisation:</li></ol><ul><li><p><strong>LL</strong>: <code>apply_poisson_balance!</code> (in <code>latlon_contracts.jl</code>) uses an FFT-based Poisson solve over the periodic-longitude grid; followed by <code>recompute_cm_from_dm_target!</code> so the explicit-<code>dm</code> closure holds at write-time replay.</p></li><li><p><strong>RG</strong>: a CG-based solve over the compressed face Laplacian, streaming previous-window state forward; the per-day binary is built window-by-window rather than accumulated.</p></li><li><p><strong>CS</strong> (spectral source): a CG-based solve on the per-panel cubed-sphere Laplacian; cm is then derived via <code>diagnose_cs_cm!</code> rather than <code>recompute_cm_from_dm_target!</code>.</p></li><li><p>All three converge to the same current dry-basis tolerance and satisfy <code>‖m_evolved − m_stored_{n+1}‖/‖m_{n+1}‖ ≤ replay_tolerance(FT)</code> at the write-time gate.</p></li></ul><ol start="6"><li><strong>Write + replay gate</strong> — <code>verify_storage_continuity_*!</code> re-runs the forward stepper on the just-written window and checks <code>‖m_evolved − m_stored_{n+1}‖ / ‖m_stored_{n+1}‖ ≤ replay_tolerance(FT)</code> (<code>1e-10</code> for Float64, <code>1e-4</code> for Float32). Failures throw rather than letting the bad binary land.</li></ol><h2 id="A-worked-LL-preprocessing-config" tabindex="-1">A worked LL preprocessing config <a class="header-anchor" href="#A-worked-LL-preprocessing-config" aria-label="Permalink to &quot;A worked LL preprocessing config {#A-worked-LL-preprocessing-config}&quot;">​</a></h2><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Preprocesses 5° (72 × 37) ERA5 spectral → daily transport binary, F32.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># (config/preprocessing/era5_ll72x37_advresln_dec2021_f32.toml)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">input</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">spectral_dir = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;~/data/AtmosTransport/met/era5/0.5x0.5/spectral_hourly&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">thermo_dir   = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;~/data/AtmosTransport/met/era5/0.5x0.5/physics&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">coefficients = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;config/era5_L137_coefficients.toml&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">output</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">directory  = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;~/data/AtmosTransport/met/era5/ll72x37_advresln/transport_binary_v4_tropo34_dec2021_f32&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mass_basis = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;dry&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">grid</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">type = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;latlon&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">nlon = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">72</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">nlat = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">37</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">level_top = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">level_bot = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">137</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">echlevs = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ml137_tropo34&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">merge_min_thickness_Pa = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1000.0</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">numerics</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">float_type   = </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Float32&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">dt           = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">900.0</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">met_interval = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3600.0</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mass_fix</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">enable                = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">target_ps_dry_pa      = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">98726.0</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">qv_global_climatology = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.00247</span></span></code></pre></div><p>The CS variant adds two <code>[grid]</code> keys (<code>Nc</code>, <code>regridder_cache_dir</code>, optionally <code>staging_nlon</code> / <code>staging_nlat</code>) and a different <code>type = &quot;cubed_sphere&quot;</code>. See <code>config/preprocessing/era5_cs_c24_transport_binary*.toml</code> for a working example.</p><p>The synthetic-RG variant uses <code>type = &quot;synthetic_reduced_gaussian&quot;</code> + <code>gaussian_number = N</code> + <code>nlon_mode = &quot;octahedral&quot;</code>; see <code>era5_o090_transport_binary.toml</code>.</p><h2 id="Performance-notes" tabindex="-1">Performance notes <a class="header-anchor" href="#Performance-notes" aria-label="Permalink to &quot;Performance notes {#Performance-notes}&quot;">​</a></h2><ul><li><p>The slowest step is <strong>spectral synthesis</strong>: at LL 720×361 it&#39;s ~30 s per window F64 on a recent 8-core CPU, ~10 s F32. Multiplied by 24 windows × 30 days, a one-month F32 run is ~2 h.</p></li><li><p>The CS variant&#39;s <strong>conservative regrid</strong> dominates at high <code>Nc</code>. <code>PreprocessorRunCache</code> builds the regridder once per batch run; the JLD2 cache (under <code>~/.cache/AtmosTransport/cr_regridding/</code>) makes the regridder persist across batch invocations.</p></li><li><p><strong>Replay-gate verification</strong> doubles the window&#39;s compute (one forward step per window). It is on by default; set the env var <code>ATMOSTR_NO_WRITE_REPLAY_CHECK=1</code> to skip (not recommended for production binaries).</p></li><li><p>All three target topologies (LL, RG, CS) run on F32 in production.</p></li></ul><h2 id="What&#39;s-next" tabindex="-1">What&#39;s next <a class="header-anchor" href="#What&#39;s-next" aria-label="Permalink to &quot;What&amp;#39;s next {#What&#39;s-next}&quot;">​</a></h2><ul><li><p><a href="/AtmosTransport.jl/stable/preprocessing/geos_native_cs#GEOS-native-cubed-sphere">GEOS native cubed-sphere</a> — the FV3-native path (no spectral synthesis, no Poisson balance; column-balanced fluxes with the raw next-hour dry endpoint).</p></li><li><p><a href="/AtmosTransport.jl/stable/preprocessing/regridding#Regridding">Regridding</a> — how the conservative weights are built and cached.</p></li><li><p><a href="/AtmosTransport.jl/stable/preprocessing/conventions#Conventions-cheat-sheet">Conventions cheat sheet</a> — units, tolerances, panel conventions.</p></li></ul>`,33)])])}const u=s(n,[["render",r]]);export{g as __pageData,u as default};
